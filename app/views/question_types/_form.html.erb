<%= form_for(@question_type) do |f| %>
  <% if @question_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_type.errors.count, "error") %> prohibited this question_type from being saved:</h2>

      <ul>
      <% @question_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id="top-area">
</div>
<br>

<form class="form-inline">
    <div class="input-group">
      <%= f.text_field :question_type_name,
      class: 'form-control', placeholder: 'Frågetyp' %>
       <span class="input-group-btn" id="search.btn-addon"><button type="submit" class='btn btn-primary'>Lägg till</button></span>
    </div>
  </form>


<% end %>
